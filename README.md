# Password Generator (Java)

## 📝 Description
This is a simple, secure command-line **Password Generator** built with Java. It uses `SecureRandom` to ensure the generated passwords are cryptographically strong.

## 🚀 Features
- Custom password length.
- Includes uppercase, lowercase, numbers, and symbols.
- Clean and easy-to-understand Java code.

## 🛠️ Technologies Used
- **Java** (JDK 8 or higher)
- **SecureRandom** class for security.

## ⚙️ How to Run
To run this project on your machine, follow these steps:

1. **Clone the repository:**
   ```bash
   git clone https://github.com
